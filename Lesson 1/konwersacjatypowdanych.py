liczba = 14
liczba = str(liczba) # zmienia typ string
print(type(liczba)) # type() odpowiada za wyswietlenie typu zmiennej czyli np. string czy float

# mozna tez zmieniac na inne typy zmiennych typu float czy intiger

liczba = int(liczba) # zmienia typ intiger
liczba = float(liczba) # zmienia typ float
liczba1 = "dziewietnascie"
liczba1 = float(liczba1) # jest to nie mozliwe bo python nie wie co to dziewietnascie